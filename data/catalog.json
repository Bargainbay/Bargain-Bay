{
  "note": "DECOMMISSIONED. This was a stale offline inventory snapshot used as a fallback. It went months out of date and twice caused Sarah/agents to report wrong inventory (e.g. 'no Blomberg, closest is Whirlpool/Beko/Cafe') as fact when a live DB read hiccuped. The Postgres products table (synced from the master tracker) is the sole source of truth. Intentionally EMPTY so no code path can ever serve stale inventory again — a failed read now surfaces an error instead of fiction.",
  "units": []
}
