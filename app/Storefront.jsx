// Deprecated: the original single-page storefront was replaced by the full
// site (app/page.jsx home + app/shop/ShopClient.jsx grid with filters).
// Kept as an empty module only because this file can't be deleted from the
// synced folder — nothing imports it. Safe to delete.
export default function Storefront() {
  return null;
}
