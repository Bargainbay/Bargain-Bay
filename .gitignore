node_modules
.next
.env
.env.local
data/reservations.json

# Vercel CLI local artifacts (contains tokens)
.vercel

# Claude Code local state and agent worktrees
.claude

# Local database backups — never commit one.
backups/
